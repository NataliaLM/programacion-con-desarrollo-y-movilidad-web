/*
    object XMLHttpRequest (xhr); encontrada Como XMLHTTP Como una interface que se
    usa para:
    1. Realizar peticiones, a servidores web, mediante directivas open();
    2. Enviar peticiones
 */

    function procesarRespuesta(){
        if (xhr)
    }